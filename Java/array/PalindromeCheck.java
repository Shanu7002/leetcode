package array;

public class PalindromeCheck {

    public static boolean isPalindrome(String word) {
        // TODO: Use dois ponteiros (left e right) para comparar os caracteres
        // Dica: word.toLowerCase() ajuda na comparação
        return false;
    }

    public static void main(String[] args) {
        System.out.println(isPalindrome("Deleveled")); // Esperado: true
        System.out.println(isPalindrome("Axur"));      // Esperado: false
    }
}